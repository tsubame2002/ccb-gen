#install

ruby version 1.9以上のみ対応

~/bash_profileに
export PATH=[scaffold/binへのパス]:$PATH
例) export PATH=~/scaffold/bin:$PATH

を追加する

#使い方

#ccbファイルを使わずに作成する場合
#ターミナルで次のコマンドを入力する

ccb-gen


#ccbファイルを使用してコマンドを作成する場合

ccb-gen example.ccb
